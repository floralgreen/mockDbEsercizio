spring:
  datasource:
    url: jdbc:h2:mem:mockdbtest
    username: testdb
    password: psw1234
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true